package com.mycompany;

public class Operadores {
    /*
    aritmeticos + - * / y % resto
    comparacion < > <= >= == !=
    logicos && y || and y or
    incremento y decremento ++ --
    asignacion = -= += /= *= %=
    negacion !
    */
}
